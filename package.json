{
  "name": "clock",
  "version": "0.1.0",
  "description": "Clock server front end base",
  "scripts": {
    "dev": "webpack -p --config webpack.config.js --profile --progress --colors --watch",
    "build": "webpack -p --config webpack.config.js --profile --progress --colors",
    "test": "jasmine-node spec/"
  },
  "devDependencies": {
      "babel-core": "^5.8.35",
      "babel-loader": "^5.4.0",
      "coffee-loader": "^0.7.2",
      "webpack": "^1.12.12",
      "jasmine-node": "^1.14.5"
  }
}
